# TODO

- [ ] Update `server.py` to add robust template-path resolution and runtime existence check for `templates/index.html`.
- [x] Deploy/re-run the app and verify `/` renders `index.html` on Render.
